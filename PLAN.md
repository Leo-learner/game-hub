# 小游戏网站后端实施方案 v2

本方案整合原方案与用户提供的参考计划。首版范围已经再次确认：游客直接游玩、不保留游客进度；用户名密码注册后使用云存档；管理员上传 ZIP 发布 HTML/JS 游戏；部署到 `games.dkz12345.com`。排行榜、成就、游客本地存档及迁移列入后续阶段。

实施授权：2026-09-25。用户指定项目根目录为 `/Users/leo/Desktop/game_site/`；本文件保留完整计划，实际交付状态见 README.md 和 docs/DEPLOYMENT.md。

## 1. 整合后的主要决策

| 项目 | 最终选择 | 原因与效果 |
|---|---|---|
| 仓库组织 | 在 `/Users/leo/Desktop/game_site/` 建立独立项目 | 按用户最新要求建立独立项目，全部源码、计划、文档及本地运行数据位于指定目录。 |
| 技术栈 | Node.js 22、TypeScript、Fastify 5、better-sqlite3 | 复用服务器环境；保留类型与接口约束，使用单进程和本地数据库。 |
| 密码与登录 | Argon2id、数据库会话、HttpOnly Cookie | 延续原方案；会话可撤销，公开注册只创建玩家。 |
| 游戏发布 | 管理员 ZIP 上传为主，CLI 导入目录为辅助 | 两种入口复用同一发布流程；不通过启动扫描自动覆盖配置或上架游戏。 |
| 游戏清单 | ZIP 可带经过版本化定义的 `game.json` | 清单描述运行入口以外的游戏包配置，数据库管理目录资料和发布状态。 |
| 游客行为 | 平台及 SDK 不为游客持久化进度 | 保留用户明确选择；游戏接入示例也不写游客本地存档。 |
| 云存档 | 多槽位、JSON、必填预期版本号、写入幂等 | 防止多设备覆盖、网络重试造成误判，以及删除重建后的旧请求覆盖新数据。 |
| 前端自由度 | REST API、OpenAPI、无界面 SDK、最小接入示例 | 首页、登录、选档、管理界面由用户实现，不绑定前端框架。 |
| 首版范围 | 账号、目录、版本发布、云存档、文档和部署 | 排行榜、成就、游客迁移不进入首版数据表、路由或 SDK。 |

## 2. 工程结构与运行边界

建议结构：

```text
game_site/
  src/
    server.ts                  # 启动、配置加载、优雅退出
    app.ts                     # buildApp(config)，便于注入测试
    config.ts
    db/                        # 数据连接、编号 SQL 迁移
    plugins/                   # 鉴权、请求防护、错误处理
    modules/
      auth/ games/ releases/ saves/
  sdk/                         # 无框架依赖的客户端与类型
  examples/example-clicker/    # 最小存档接入示例
  scripts/                     # 管理命令、部署、备份恢复
  deploy/                      # systemd、Nginx 配置
  docs/API.md
  test/
  public/                      # 用户后续放置前端构建产物
  package.json
  .env.example
  README.md
```

- 路由只负责输入输出、权限和调用服务；游戏导入、发布、存档等业务规则集中在服务层，CLI 与 HTTP 接口共用。
- 请求与响应采用共享 JSON Schema，作为运行时校验、OpenAPI 和 SDK 类型的来源；不分别手工维护三套接口定义。[Fastify Swagger 官方说明](https://github.com/fastify/fastify-swagger)
- `buildApp(config)` 接收独立的数据目录、时钟和测试配置；测试不依赖生产目录、真实域名或全局环境变量。
- 应用与 SDK 的 TypeScript 分别构建为服务器代码和浏览器代码；普通 JavaScript 游戏同样可以直接接入。
- 数据库、游戏包、备份、会话密钥不进入 Git；项目 `.gitignore` 已为 `.env.example` 添加例外。
- 新服务部署到独立应用目录，不放入现有门户的静态根目录。门户现有部署也应使用明确文件清单，避免把新后端子目录公开复制出去。

## 3. 游戏包、清单和发布流程

### 3.1 包格式及唯一配置来源

ZIP 根目录必须包含 `index.html`，游戏资源使用相对路径，可包含 JS、CSS、图片、音频、JSON 等静态文件。服务器不运行包内的安装脚本、构建命令或服务端程序。

可选清单：

```json
{
  "manifestVersion": 1,
  "cover": "cover.svg",
  "saveSchemaVersion": 1,
  "saveSlots": 10
}
```

- 无清单时默认入口 `index.html`、存档格式版本 1、10 个存档槽、无封面。
- `slug`、名称、简介、标签、展示顺序由管理员创建和编辑，数据库是唯一来源；`slug` 创建后不可修改，标题可以修改。
- 清单只描述该版本的封面路径、存档格式版本及槽位能力；清单随版本不可变，发布时成为当前配置。
- 清单不能设置管理员、发布状态、服务器目录或任意响应头；不接受包内 `published: true` 自动上架。
- 上传、CLI 导入都先创建草稿版本，再显式发布。目录导入使用 `games import <directory> --game <slug>`；不会在服务启动时扫描目录并覆盖数据库。
- 首版全局槽位上限默认 10、每槽 1 MiB，可通过配置调整；单个游戏可在清单中声明不超过全局上限的槽位数。下调上限不删除旧档，已有槽可继续读取和更新，上限约束新增槽位。

### 3.2 上传和原子发布

1. 管理员创建游戏资料，得到稳定的 `id` 和 `slug`。
2. 流式上传 ZIP 到临时目录，默认压缩包最多 50 MiB、解压后总计 200 MiB、最多 10,000 个文件，同一时间只处理一个解压任务。
3. 校验入口、清单、封面、文件实际大小与路径；拒绝绝对路径、路径穿越、符号链接、重复或冲突路径、隐藏配置和损坏压缩包。
4. 计算包摘要，将完整文件集原子移动到不可变版本目录，再记录 `ready` 版本；失败时清理临时文件，当前版本继续服务。
5. `publish` 接收 `releaseId`，在事务中切换当前版本。游戏目录、封面和启动地址统一指向该版本。
6. 回滚通过再次发布历史版本完成；不覆盖版本目录、不回滚玩家存档。

资源地址为 `/play/{slug}/{releaseId}/index.html`。后端先检查游戏发布状态和版本资格，再通过 Nginx `X-Accel-Redirect` 发送文件；内部文件目录不能绕过鉴权直接访问。

更新后，曾发布的历史版本仍可为已打开的页面提供资源，避免混用不同版本文件；未发布草稿不公开。下架后新的启动和资源请求返回 404，不能撤回用户已经下载的内容。现有玩家仍可管理自己的存档，已打开页面也可正常保存。

静态资源使用需要重新验证的缓存策略，验证请求同样先检查发布状态；不对受发布状态控制的资源设置长期免验证缓存。SPA 回退只适用于前端页面，不得吞掉 API、SDK、游戏文件的 404。

## 4. 账号、会话与存档行为

### 4.1 账号与会话

- 用户名 3–20 位 ASCII 字母、数字或下划线，大小写不敏感唯一；昵称支持中文，1–24 个字符；密码 8–128 个字符。
- 注册成功立即登录。`GET /auth/me` 对游客返回 `{ "user": null }`；需要登录的业务接口仍返回 401。
- 提供退出当前会话、退出所有会话、修改昵称和修改密码；修改密码后撤销其他会话并轮换当前会话。无邮箱找回流程，由管理员通过 CLI 重置密码并撤销全部会话。
- 会话令牌使用 32 字节安全随机数，数据库只存 SHA-256 摘要。Cookie 仅属于当前主机，不设置父域 `Domain`；生产环境启用 `HttpOnly`、`Secure`、`SameSite=Lax`。
- 默认会话闲置有效期 30 天，最多每 24 小时续期一次，避免每个资源请求都写数据库；过期会话定期清理。
- 管理员通过 CLI 初始化；注册请求中的角色字段无效并被拒绝。密码和会话信息不出现在普通日志或公开响应中。
- 所有状态变更请求，包括登录、退出、空请求体操作与 multipart ZIP 上传，统一要求 `X-GameHub-Request: 1`；Origin 存在时必须精确匹配允许来源，`Origin: null` 拒绝。生产只允许当前站点来源，开发来源逐项配置，禁止通配子域。缺失 Origin 的 CLI 请求也必须带自定义头；无该头的简单表单请求一律拒绝。此方案使用自定义请求头与严格 CORS 预检防护，不将“请求体是 JSON”作为唯一条件。[OWASP API 请求防护说明](https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html)
- 后端只监听回环地址，仅信任回环代理；Nginx 覆盖客户端传入的转发头。注册、登录、存档及上传分别限流，避免把任意 `X-Forwarded-For` 当作真实客户端地址。[Fastify 代理配置说明](https://fastify.dev/docs/latest/Reference/Server/#trustproxy)

### 4.2 云存档

- 存档归属从服务端会话取得，客户端不能指定写入其他玩家的数据。数据库唯一键为玩家、游戏、槽位。
- 槽位使用字符串标识，例如 `auto`、`slot-1`；最多 32 个 ASCII 字母、数字、连字符或下划线。前端自由决定展示几个槽、如何命名以及何时保存。
- `data` 为 JSON 对象，后端不解释游戏内容。`schemaVersion` 表示游戏存档格式，`revision` 表示服务器的并发控制版本，两者独立。
- 读取不存在的槽返回 404；列表仅返回槽位、大小、格式版本、修订号和更新时间，不携带完整存档数据。
- `expectedRevision` 必填：0 表示创建当前不存在的槽，更新必须等于当前修订号。事务内完成条件写入和递增，冲突返回 `409 SAVE_CONFLICT`，不提供绕过校验的强制覆盖参数。
- 删除同样要求预期修订号；保留删除标记和递增版本，重新创建后不从 1 重置，防止删除前的旧请求误写新存档。
- 大小以 JSON 序列化后的 UTF-8 字节数计算；超限返回 413，新增槽位超限返回 `409 SAVE_SLOT_LIMIT`。
- 写入接受 `Idempotency-Key`，SDK 每次逻辑保存自动生成并在重试时复用。服务端按玩家、资源、键及请求摘要保存 24 小时的结果凭据；同键同内容返回原写入结果，同键不同内容返回冲突。幂等记录与存档写入在同一事务提交，记录仅保存响应元数据，不重复存储完整存档。
- 游戏更新不清空存档。恢复旧格式时由游戏执行迁移，再按原修订号保存；不兼容时应由前端提示，后端不自动丢弃旧数据。

保存请求：

```json
{
  "data": { "level": 3, "score": 1200 },
  "schemaVersion": 1,
  "expectedRevision": 0
}
```

保存响应返回 `{ slot, schemaVersion, revision, updatedAt, sizeBytes }`。`updatedAt` 始终由服务器生成，使用 UTC ISO 8601；删除请求通过查询参数 `expectedRevision` 传入版本。

## 5. 首版接口契约

业务前缀统一为 `/api/v1`，JSON 字段使用 camelCase。游戏路由统一使用不可变 `slug`；响应仍包含内部 `id`。不同时维护另一套无版本号 `/api/*` 接口。

| 方法 | 路径（省略 `/api/v1`） | 行为 |
|---|---|---|
| POST | `/auth/register` | 注册并设置登录 Cookie |
| POST | `/auth/login` | 登录并轮换会话 |
| POST | `/auth/logout` | 退出当前会话 |
| POST | `/auth/logout-all` | 退出全部会话 |
| GET | `/auth/me` | 当前用户，游客为 `user: null` |
| PATCH | `/auth/me` | 更新昵称 |
| PUT | `/auth/password` | 校验旧密码后修改密码 |
| GET | `/games` | 已发布游戏列表，支持 `q`、`tag`、`page`、`pageSize` |
| GET | `/games/{slug}` | 已发布游戏详情及当前 `launchUrl` |
| GET | `/me/saves` | 当前玩家跨游戏的存档摘要，便于制作个人中心 |
| GET | `/games/{slug}/saves` | 当前玩家该游戏的存档槽摘要 |
| GET | `/games/{slug}/saves/{slot}` | 读取完整存档 |
| PUT | `/games/{slug}/saves/{slot}` | 带版本条件创建或更新存档 |
| DELETE | `/games/{slug}/saves/{slot}` | 带版本条件删除存档 |
| GET / POST | `/admin/games` | 查询全部游戏，包括草稿；创建游戏 |
| GET / PATCH | `/admin/games/{slug}` | 查看和编辑游戏资料 |
| GET / POST | `/admin/games/{slug}/releases` | 查询版本；multipart 上传 ZIP |
| POST | `/admin/games/{slug}/publish` | `{ releaseId }`：发布或切回已有版本 |
| POST | `/admin/games/{slug}/unpublish` | 下架，保留版本与玩家数据 |

分页默认每页 20 条、最多 100 条，列表返回 `{ items, page, pageSize, total }`。公开游戏列表按展示顺序及 slug 稳定排序。

游戏详情至少返回 `id`、`slug`、`title`、`description`、`tags`、`coverUrl`、`launchUrl`、当前版本和存档能力；前端不自行拼接磁盘路径或版本地址。用户私有数据及身份响应设置 `Cache-Control: no-store`。

统一错误示例：

```json
{
  "error": {
    "code": "SAVE_CONFLICT",
    "message": "存档已在其他设备更新",
    "details": { "currentRevision": 8 }
  },
  "requestId": "..."
}
```

文档须列明 400 参数错误、401 未登录、403 无权限或请求防护失败、404 不存在、409 状态冲突、413 大小超限、429 限流和 500 服务异常。响应不暴露堆栈、SQL 或服务器文件路径。

其他保留路径：`/docs` 为接口文档，`/openapi.json` 为机器可读契约，`/sdk/` 为客户端构建产物，`/play/` 为游戏资源，`/healthz` 和 `/readyz` 为存活及就绪检查。其余页面路由交给前端。

## 6. 数据与 SDK

首版数据表仅包含：

- `users`：用户、昵称、密码摘要、角色。
- `sessions`：会话摘要、用户、有效期与续期时间。
- `games`：稳定标识、目录资料、发布状态、当前版本。
- `game_releases`：游戏版本、包摘要、不可变清单、资源位置、发布时间。
- `saves`：玩家、游戏、槽位、JSON、大小、格式版本、修订号、删除标记。
- `idempotency_keys`：写入请求摘要、结果元数据和过期时间。

数据库启用外键和 WAL；用编号 SQL 迁移及版本表记录已执行迁移。迁移只在部署阶段单独运行，避免多个服务进程同时迁移。写事务保持短小，不把上传、解压或网络操作放入事务。WAL 仍是单写者模型，不能把它当作多写进程扩展方案。[SQLite WAL 说明](https://www.sqlite.org/wal.html)

SDK 提供 ESM 和普通 `<script>` 两种入口，包含账号、目录、存档及管理员 API；无需采用 SDK，也可直接用 fetch 调用同一 REST 接口。

- 提供 `createClient({ baseUrl })`，自动携带 Cookie、请求防护头、幂等键，并返回类型化结果和错误。
- 存档通过 `load`、`save`、`remove` 显式调用；自动保存可使用可选的防抖帮助函数，保存时机和选档 UI 由游戏决定。
- SDK 不写 localStorage/IndexedDB，不把登录凭证暴露给游戏 JS。云端失败、过期登录、冲突均返回明确错误，不静默切换为本地存档。
- 内存中的存档缓存和待保存任务按账号、游戏、槽位隔离；退出或切换账号时清理待发送队列，避免把上一个玩家的进度写入新账号。
- 示例游戏演示：游客游玩 → 用户通过无样式测试入口注册/登录 → 保存当前内存状态 → 刷新后恢复。示例仅用于集成验证，正式页面由用户设计。

## 7. 部署、备份与交付

已只读核实的环境：Ubuntu 22.04、Node.js 22、Nginx、约 1 GB 内存；`games.dkz12345.com` 已解析到现有服务器。实施时再次检查资源和端口，避免依赖过期状态。

1. 在用户指定的 `/Users/leo/Desktop/game_site/` 开发；本地完成构建、测试、契约和 SDK 验证。
2. 使用锁文件安装依赖；Linux 上安装对应生产依赖，不能复制 macOS 的 `node_modules`。应用以专用低权限用户和单个 systemd 进程运行，监听 `127.0.0.1:3220`。
3. 代码采用版本目录与 `current` 链接；数据库、游戏包、临时上传及备份保存在代码目录之外，升级不会覆盖玩家数据。
4. 上线前备份，停止该新服务执行数据库迁移，切换应用版本后通过就绪检查；迁移失败不继续切换。回滚到兼容数据库结构的代码版本，数据库恢复使用明确的维护流程。
5. 新建独立 Nginx 站点并配置 HTTPS；上线前执行配置检查，后端和 Nginx 的上传大小限制保持一致。前端根目录预留，正式 UI 完成前只使用文档和示例验证后端。
6. CLI 提供 `create-admin`、`users list`、`reset-password`、`games import`、`backup` 和恢复校验；敏感密码从交互输入读取，不作为命令行参数、日志或仓库配置保存。
7. 每日使用 SQLite 在线备份 API 生成一致快照，并保存快照引用的不可变游戏版本及校验清单。默认保留 7 天；不直接复制运行中的数据库主文件代替在线备份。[better-sqlite3 备份 API](https://github.com/WiseLibs/better-sqlite3/blob/master/docs/api.md)
8. 备份保存在服务器非公开目录，权限仅允许运维账号访问；同机备份用于操作失误恢复，后续如需应对整机磁盘损坏，再配置异机副本。

部署后用独立测试账号和示例游戏完成小流量烟测，验证现有站点仍正常访问。交付源码、中文接口文档、OpenAPI、SDK、示例、环境变量示例、部署配置、备份恢复说明和线上地址。

代码实施完成时可以汇总为独立 PR，提交内容只涉及后端子目录及必要的仓库说明；参考文本中的特定分支名不作为本方案约束。当前用户已授权实施及部署；后续服务器发布遵循用户既定流程：本地验证后推送 GitHub，再部署该提交，不创建额外 PR。

## 8. 验收清单

使用 `node:test`、Fastify `app.inject()` 和真实临时 SQLite 文件进行接口集成测试，浏览器验证 Cookie、SDK 和资源加载行为。

1. **账号**：注册、重名、错误密码、游客 `user: null`、会话续期、退出全部会话、密码修改与 CLI 重置后旧会话失效。
2. **请求防护**：恶意来源、缺少自定义头、multipart 表单提交、普通玩家调用管理接口及伪造代理头均按契约处理；合法开发来源和文档示例可用。
3. **存档**：跨账号和跨游戏隔离；列表不泄露正文；新建、更新、删除；字节及槽位限制；重启后持久化。
4. **并发与重试**：同一修订号竞争时仅一个写入成功；遗漏版本号拒绝；删除重建后旧请求失败；断网重试同一幂等键返回原结果；同键不同内容拒绝。
5. **上传与发布**：合法 ZIP、入口和清单校验、恶意路径、符号链接、超限与损坏包、失败清理、发布切换、旧页面加载、回滚、下架旧链接拒绝及存档保留。
6. **契约与 SDK**：所有路由都有请求响应与错误定义；OpenAPI 可解析并生成 SDK 类型；原生 HTML 和普通 fetch 均能完成登录和读写存档；切换账号不串档。
7. **运维**：空库迁移、重复启动、优雅退出、健康检查、错误日志脱敏；从备份恢复到空目录后可以恢复账号、游戏版本及存档。
8. **上线**：HTTPS、Cookie 属性、代理路由、上传限制和静态资源类型正确；前端页面回退不会吞掉 API 404；部署不影响现有门户及其他服务。

## 9. 后续扩展边界

排行榜、成就、游客本地存档及一键迁移已明确放到后续阶段，首版不创建空表、不暴露占位接口。

- 排行榜启用时再确定榜单周期、时区、同分规则和成绩可信程度；客户端自报成绩不能仅因通过数值范围校验就视为已验证成绩。
- 成就系统启用时再定义解锁依据、隐藏内容的公开边界及解锁率分母。
- 游客迁移启用时再增加本地存储适配器；迁移复用云存档版本条件和幂等规则，不根据浏览器提供的时间戳自动覆盖云端进度。
- 公开玩家资料、游玩次数和最近游玩记录随具体前端需求加入，不与首版存档和账号能力绑定。

## 10. 实施完成记录（2026-09-25）

已按本计划完成账号、游戏目录、管理员 ZIP 上传、版本发布/回滚/下架、多个云存档槽、SDK、OpenAPI、中文文档、HTTPS 部署及每日备份。正式前端保留可替换入口。

实际工程将鉴权和业务服务放在 src/modules/*.ts，路由/插件装配集中在 src/app.ts；没有为了目录形式额外拆分空模块。接口类型由 Schema 生成，业务实现由 CLI 和 HTTP 共用。

13 组自动化测试、31 项线上 HTTP 检查、真实浏览器联调、证书续期演练及备份恢复演练已通过。SDK 自动保存增加成功回调以便前端更新 revision，退出后丢弃迟到的保存结果。实际应用版本、示例地址、权限和凭据位置见 docs/DEPLOYMENT.md。

排行榜、成就、游客迁移继续作为后续扩展，不进入本次交付。


## 11. MCP 维护与运营实施计划

本次新增远程 Streamable HTTP MCP，服务 Codex 与 Claude Code。以独立 TypeScript 进程运行于回环端口 3221，经现有 HTTPS 站点 `/mcp` 转发。复用 GameService、ReleaseService、AuthService、backup/verifyBackup，保持前端自由和现有浏览器 API 契约。

1. **工具定义**：17 个明确工具，游戏资料/上传/发布/回滚/下架，普通玩家查询/密码重置/会话撤销，状态/备份/校验/审计/日志/重启；提供资源与巡检提示词。参数、返回和错误见 docs/MCP.md；tools/list 是机器可读契约。
2. **授权与数据**：独立随机 Bearer 密钥，数据库只保存摘要；四类范围 read/content/accounts/ops，默认 90 天、最长 365 天，支持立即撤销。MCP 专用表由显式迁移管理，与 API schema 1 兼容。
3. **游戏包**：工具返回一次性、10 分钟有效的上传票据；HTTP 流式上传 ZIP，沿用大小、路径、清单和摘要验证，生成草稿版本后显式发布。保留上传状态支持不确定结果核查。
4. **维护边界**：不开放任意 Shell/SQL/文件写入、用户删除、备份恢复或读取存档正文。固定 root 所有的助手只读取指定服务日志和状态、重启网站 API。MCP 自身保持在线。
5. **可追溯性**：每次工具调用先记录 started/running，再更新成功或错误；不记密码/密钥/参数正文。账号工具不得操作管理员。游戏、昵称、日志视为不可信数据。
6. **测试**：官方 SDK 的真实 HTTP Client，兼容旧版协议；覆盖范围隔离、撤销与过期、Host/Origin、匿名拒绝、体积限制、上传重放和失效、发布回滚、管理员保护、密码/会话撤销、审计脱敏、备份及路径边界。保留原 API/SDK 回归测试。
7. **发布**：本地检查 → 提交并推送独立私有 GitHub 仓库 → 从该提交归档 → 上传服务器安装。保留 RELEASE.json 提交标识、升级前备份和回滚路径；验证公网 MCP、网站 API 和原门户。
8. **交付**：MCP 源码、systemd/Nginx/固定助手、凭据管理和上传脚本、MCP.md、公开配置模板与私有填好密钥的配置片段。提供用户自行合并的配置说明，不擅自改动 Codex/Claude Code 全局设置。

本次不扩展前端、排行榜、成就、游客迁移或访客分析。数据库账号数和存档数不当作访问量或游玩次数。
